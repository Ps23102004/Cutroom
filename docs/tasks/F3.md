# F3: truthful Settings and fixture affordances
Owner: Gemini3.8 Flash via AGY. Fresh small task, no master/history reread. Own ONLY apps/desktop/src/routes/SettingsRoute.tsx, apps/desktop/src/components/shell/TopBar.tsx, apps/desktop/src/__tests__/SettingsTruth.test.tsx, docs/evidence/F3.md. Root owns Cutline; do not touch it or lockfiles.

Read docs/evidence/FRONTEND-FINISH-REVIEW.md when present, plus named source, packages/ui controls. Native backend is genuinely not implemented. Browser is explicitly unavailable for production mutations. Fix Settings truth defects:
1. Remove route-local fake reducedMotion/highContrast switches. Show clear read-only information: reduced-motion follows system/browser preferences (Cutline actually subscribes), enhanced contrast custom override unavailable. Do not imply preference saved or claim all transitions disabled. Do not add state merely to move a switch.
2. Storage: remove fabricated 124GiB/APFS and editable fake workspace/cache paths. Show path and capacity as unavailable until desktop engine can report them; no invented defaults.
3. Models/media: replace hardcoded CoreML/NeuralEngine, VideoToolbox, FFmpeg detected, R128/truepeak pipeline with runtime-unavailable readings. Developer preflight actual tools are not installed-product detection. No successful capabilities without actual result.
4. Privacy: replace Local-First Guarantee promotional copy with accurate current preview behavior. Remove disconnected telemetry switch; state diagnostics sending unavailable. No promise never backed by tests.
5. Diagnostics: disable no-handler button with adjacent explanation engine unavailable, no invented sanitized output.
6. TopBar Load Sample Fixture affordance must be inside import.meta.env.DEV guard (handler already gated). Replace No Tauri wording with plain engine unavailable.
7. Test Settings no hardcoded hardware/capacity claims, unsupported diagnostics disabled, truthful preference info. Meaningful tests, no snapshots mirroring markup.

Preserve exact8route manifest, shared shell/token/components. No backend replacement, newfeatures, installs, imagegen, network, skip-permissions. Run desktop typecheck and targeted SettingsTruth tests. Timeout bounded4minutes; concise final with exact evidence and limitations, not all-feature completion. If test fails fix it, do not claim pass from file presence.
