# F1 — Gemini 3.8 Flash shared shell and connected UI foundation

Parent Phase 1/3. Base: new repository, explicit file ownership. Own apps/desktop/** EXCEPT src-tauri/**; packages/ui/**, packages/design-tokens/**, root package.json and package lock (single frontend dependency owner), docs/SCREEN_INVENTORY.md, docs/evidence/F1.md. Do not edit Rust crates, native config, other evidence, agent ledger or source reference files.

F0 design reviewed by Astra; correct these before treating as approved: no unmeasured WCAG assertion; tertiary text only when contrast measured. Remove noise gate (not implemented), generic cloud sync, unimplemented pause or remaining-time controls. No rendered production capabilities or certifications until native tests. Do not call documentation committed unless Git commit exists. Google generate_image product ID still unverified; don't claim Nano Banana model identity from tool name.

Implement the shared real React TypeScript Vite UI foundation using the pinned charcoal/plum world in docs/DESIGN_CONTRACT.md and PRODUCT.md. User explicitly settles routine design decisions internally, so skip user interviews/concept-page/image UI generation that conflict with mandate. Read installed Impeccable craft-floor for practical verification. One 216px sidebar, 56px header, eight exact routes plus Jobs/Help footer, single route manifest. Accessible controls, matte content, glass navigation only, focused subviews. Useful first-run/no-project/missing-backend state. No fake project/activity/model data in production. Optional labeled fixtures must be explicit opt-in.

Build actual forms/workflows for create/open project, import media, composition list/trim/reorder/remove, save revision, render/jobs, versions, with typed async state and native errors. More focused routes may show honest pending capability text while backend grows. Do not wire unsupported buttons to success toasts.

Provisional native transport: Tauri invoke('dispatch', {request}) with JSON request {command:string, operation_id?:string, project_id?:string, expected_version?:number, payload:object}; response {ok:true,data:T} or {ok:false,error:{code,message,details?}}. Exact commands/payloads will follow backend CONTRACTS.md; keep adapter isolated in src/lib/native.ts. Browser mode must show native-unavailable, no localStorage fake database. No local HTTP backend.

Initial command names to coordinate: project.list/create/open; asset.import/list; composition.get/apply; revision.create/list/restore; render.enqueue; job.list/cancel/retry; health.get. Backend contract will supersede provisional shapes.

Review package scripts before dependency execution, pin project dependencies; reuse package cache where possible. Allowed reviewed local installs/build/typecheck/component tests. No global config changes, no auto-approval gates or dangerous flags. If a human gate blocks a command report it and continue independent edits. Expected working source, build/test logs with exit codes and a concise report. No image generation in this packet. No private media, external fonts, hosted services, publication or charges.
