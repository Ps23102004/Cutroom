# F2 — Gemini bounded completion of UI foundation

Fresh task; read this packet, relevant existing source and docs/ASSET_PLAN.md only. Do not reload full mandate or earlier conversation. F1R timed out after changes; Astra independently ran typecheck and all38 renderer tests successfully, but integration defects remain. Same frontend/package ownership as F1; do not edit backend, scripts or asset generators.

1. Finish factual corrections: remove all remaining 'certified' output claims and implementation packet names B1 from user-facing Help/Deliver/AI Briefs. Update docs/evidence/F1.md headline/conclusion to 'frontend foundation; native workflow blocked' with actual38-test evidence, not complete product compliance. Canonical rational timeBase must be explicit/required in asset/composition types and fixtures; forbid invalid time-base input. Provisional native transport is not an implemented backend.
2. Add a small lazy Cutline component on Home only using the actual assets/3d/fallback/cutline-ribbon-fallback.glb. This is explicitly procedural, not converter-generated. Existing Three version0.185.1 may be added as exact project dependency (root lockfile remains yours); no absolute toolkit imports in shipping code. Static fallback from assets/3d/fallback/cutline-rest-snapshot.png. Renderer import must be lazy and outside project-open critical path. One small decorative placement, no gallery or scene controls in the product. Default idle static; pointer may schedule a short orientation response only. Reduced-motion/transparency preference yields static fallback. Stop hidden/offscreen; dispose GPU resources; DPR<=1.5. Never continuous requestAnimationFrame while editing/rendering. No fake job state. Use GLTFLoader correctly, canvas aria-hidden. Handle WebGL/load error with static fallback and keep main UI usable.
3. Run build/typecheck/tests and one actual GLB-loaded scene check if possible. Report measured bundle sizes, not expected claims. No screenshot proof of native workflow. Make one bounded implementation pass, no new features. Return <=30 lines plus artifact paths. Output report docs/evidence/F2.md. Keep permission gates intact; do not introduce unsafe flags or new chargeable services.
