# B0 — GLM 5.2 backend foundation

Parent Phase 0/3. Base: new repository, no prior commit. Own Cargo.toml, Cargo.lock, crates/**, docs/ARCHITECTURE.md, docs/DATA_MODEL.md, docs/CONTRACTS.md and docs/evidence/B0.md. Do not edit apps/**, packages/**, other docs or frontend lockfiles.

Read references/MASTER.md sections 6, 10–14 and Phase 3. Implement Rust workspace and cutroom-core library with SQLite migrations and relational project/asset/working composition/immutable revision/operation receipt/job records. Single execution owner, no Node local daemon. Rational ticks encoded as strings for JSON. Typed operation/error envelope; expected version conflict; idempotency digest detects reused operation IDs with changed payload. Enforce workspace scope, source ranges, clip-instance identity and backend allowlisted editing operations. Save/reopen immutable content-hashed revisions. No shell/SQL/path in model operations. Add native-ready service API and a bounded test CLI if useful.

First return contracts and implementation, not advice. Tests must exercise reopen across independently opened stores, stale writes, ID reuse, invalid ranges, immutable revision hashing, transaction rollback. Job running lease recovery required in next packet if not here. Allowed project-local edits and Rust test commands; inspect dependencies/scripts, use cached dependencies first. No global installs, downloads of models, public network side effects, credentials or privilege bypass. Record exact commands/results. Coordinate IPC contract through docs/CONTRACTS.md and notify Astra when ready. Keep scope bounded; media follows after this gate.
