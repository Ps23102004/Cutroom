# F4 bounded frontend truth correction
Owner Gemini3.8Flash via AGY. Own ONLY apps/desktop/src/routes/DeliverRoute.tsx, apps/desktop/src/components/drawers/HelpDrawer.tsx, apps/desktop/src/__tests__/DeliveryTruth.test.tsx, docs/evidence/F4.md. Read these plus docs/evidence/FRONTEND-FINISH-REVIEW.md (appended findings may arrive). No master/history reread.

DeliverRoute unconditionally hardcodes four passed preflight checks, 124GiB, source hashes, fonts and normalization despite no backend. Replace with neutral NOT CHECKED/UNAVAILABLE entries and remove every positive Passed/Static Profile Ready or machine-verified claim. Presets may show planned targets clearly, but no certified/ready/verified capability until actual backend result. Scope has no backend preflight operation: do not invent one. Disable render submission unless native backend connected; do not make synthetic fixture job enqueue look like real render. Keep exact shell/routes; no new features. Tests explicitly load DEV fixture activeProject then visit preflight to assert no passed measurements, and unavailable status, no fake disk capacity. Use existing AppProvider test patterns.

HelpDrawer must only document actual behavior. Remove unimplemented keyboard shortcuts (Cmd+Z, Cmd+S,1-8 etc; inspect actual handlers in StudioRoute before keeping any). Native revision docs state unavailable/planned, no current immutable guarantee. Offline interface navigation help may explain Tab/ShiftTab/Enter/Escape where supported, search only filters these bundled short articles. No AI assistant, ticket/support/repair or diagnostics success claims. Scope small and truthful. No images/downloads/installs/native work.

Run desktop typecheck and targeted DeliveryTruth test, fix failures. Keep response <400words with executed evidence; not complete product claim. 3minute bound.

Reviewer final added: same Deliver route delivered-packages panel invents artifact/digest and no-handler Finder. Replace with truthful empty package list and disabled/absent Finder action. Include absence of fake delivery artifact/hash in test. This is part of F4, same owner/file.
