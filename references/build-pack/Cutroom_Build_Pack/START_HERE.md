# Start the Cutroom build

This packet is an implementation mandate, not application source or a claim of completed work. Its master prompt contains the complete product, design, agent, privacy, support, testing, and release instructions.

## Use

Open GPT-6 Astra in the existing tool-enabled coding environment, attach `Cutroom_GPT6_Astra_Master_Build_Prompt.md`, and make the current design ZIP(s) accessible in the project workspace. The orchestrator must inspect the actual files and installed tools; their names and paths were not verified in this chat.

Paste the launcher below. The master prompt is self-contained; the other packet files are structured starting templates and historical context, not additional installation requirements.

```text
You are GPT-6 Astra, orchestrating the complete Cutroom build for Parth.

Read the entire attached/local file
Cutroom_GPT6_Astra_Master_Build_Prompt.md before implementation.
Treat it as the build mandate. Read it in sections if necessary; do not
replace it with a summary or implement only the first few sections.

We are building the local-first creator production studio described
there: a working desktop app and native backend, media processing,
local AI workflows, persistent versions, browser review, exact-version
approvals, delivery, customer support, recovery, and release evidence.

Inspect the actual repository, design ZIPs, installed CLI wrappers,
image tools, local models, and image-to-3D tool before changing them.
Reuse healthy installations and preserve all working routes and auth.
Do not install everything in the older toolchain setup packet.

Use real worker calls, not simulated agent roles:
- GLM 5.3: planning/review partner with you.
- GLM 5.2: backend, persistence, media/jobs, runtime AI, and tests.
- Gemini 3.8 Flash through AGY/Antigravity: frontend, UX, shared UI,
  asset planning, and integration with GLM 5.2.
- GPT-5.6 Luna: bounded automation, test operations, research, and
  authorized use of existing image/tool routes.
- GPT-5.6 Terra, called Tara by me: narrowly scoped escalations only.

Keep Astra mainly on decisions, delegation, integration, and review.
Resolve exact installed model names and record real participation.
Do not quietly fall back to doing all the coding yourself.

First settle the information architecture, shared shell, feature
placement, and design contract from my current design files. Keep the
minimal charcoal/plum, violet, ochre, maroon, sky-blue, and light-green
direction. Use glass for navigation/controls, not all content panels.
One shared sidebar, top bar, route manifest, and component system must
keep every desktop view consistent.

Then use the actual Nano Banana 2/Nano Banana Pro and GPT Image 2 tools
for planned object studies. Use my installed image-to-3D tool to create
a real mesh, repair and animate it, and provide a static fallback.
A restrained timeline-inspired Cutline strip is the default direction;
no unnecessary dragon, orb, asset-marketplace page, or palette poster.

Build-time image generation is separate from product functionality.
Do not invent generative video, 3D imports, or other editing features
because old mockups contain them.

The shipped runtime should evaluate Gemma 4 E4B/E2B first, use a stronger
local fallback only when warranted, and enforce permissions and edit
validation in code. Customers must not need my coding subscriptions.
Include a grounded local support agent, offline help, safe diagnostics,
and honest consented ticket handling—not just a chat bubble.

Luna may operate actions within already authorized scope. It must not
blindly grant permissions, impersonate my approval, or bypass AGY/OS
human approval gates. Batch genuine access/spending/publication blockers
and continue unrelated work.

Create concise build state, role/capability records, feature traceability,
and actual test evidence. Then execute the phased plan in the master.
Do not stop at a plan, static design, or shell. Continue through working
vertical slices and review gates. At a real limit, save exact state and
the next action; never claim work continues outside the active session.

Begin with preflight and real worker delegation now.
```

## Files

- `Cutroom_GPT6_Astra_Master_Build_Prompt.md`: authoritative implementation mandate.
- `FEATURE_ACCEPTANCE_STARTER.json`: planned requirements and test obligations; no completion claims.
- `ROLE_ROUTING_STARTER.json`: requested team with unresolved local routes.
- `AGENT_TASK_TEMPLATE.md`: bounded delegation and evidence format.
- `ASSET_REQUEST_TEMPLATE.md`: asset brief and generator/converter handoff format.
- `DESIGN_CONFLICTS.md`: corrections to the earlier generated concepts.
- `historical/Original_Blueprint_v0.1_NOT_CURRENT.md`: original proposed blueprint, subordinate to the master and latest owner instructions.

Nothing in this packet grants additional host permissions, spends money, installs packages, or publishes Cutroom automatically.
