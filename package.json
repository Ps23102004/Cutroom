{
  "name": "cutroom-monorepo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@9.15.9",
  "scripts": {
    "build": "pnpm -r run build",
    "typecheck": "pnpm -r run typecheck",
    "test": "pnpm -r run test",
    "dev": "pnpm --filter @cutroom/desktop dev"
  },
  "devDependencies": {
    "typescript": "5.7.2"
  }
}
